[project]
name = "project"
version = "0.1.0"
description = "spark medallion data pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=4.1.1",
    "pyyaml>=6.0.3",
]
